// string
// Comillas simples
var firstName = 'Sergio Antonio';
var lastName = 'Ochoa Martinez';

var firstNameUpper = firstName.toUpperCase();
var lastNameLower = lastName.toLowerCase();

var countWord = firstName.length;

var firtWord = firstName.charAt(0);
var lastWord = firstName.charAt(firstName.length - 1);

var str = firstName.substring(1,2);

// Template String
var fullName = `${firstName} ${lastName}`;
