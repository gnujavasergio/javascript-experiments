let developer = {
    'firstName': 'Sergio',
    'lastName': 'Ochoa',
    'age': 33,
    'country': 'BO',
}

console.log(developer);

// Dejar campos vacios
const array = [24,56,78,,,,45];
console.log(array);
console.log(array.length);
