// Concatenacion
let firstName = 'Sergio';
let lastName = 'Ochoa';

// ES5
let fullName = firstName + '' + lastName;
console.log(fullName);

// ES6
let full = `${firstName} ${lastName}`;
console.log(full);