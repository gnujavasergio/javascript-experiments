// Un contador
var value = 5;
var contador = 0;

// Asignación de adición short
contador += value;
contador += value;
console.log(contador);

// Asignación de adicion full
contador = contador + value;
console.log(contador);